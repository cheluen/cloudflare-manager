# =============================================================================
# Cloudflare Manager Skill - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# IMPORTANT: Never commit your .env file to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED: Cloudflare API Token
# -----------------------------------------------------------------------------
# Get your API token from: https://dash.cloudflare.com/profile/api-tokens
#
# RECOMMENDED SETUP:
# 1. Click "Create Token"
# 2. Select "Create Custom Token"
# 3. Configure the following permissions based on features you need:
#
# ┌────────────���────────────────────────────────────────────────────────────────┐
# │                        REQUIRED PERMISSIONS                                  │
# ├─────────────────────────────────────────────────────────────────────────────┤
# │ Feature              │ Permission                          │ Access Level  │
# ├──────────────────────┼─────────────────────────────────────┼───────────────┤
# │ Workers deployment   │ Account > Workers Scripts           │ Edit          │
# │ KV Storage           │ Account > Workers KV Storage        │ Edit          │
# │ R2 Storage           │ Account > Workers R2 Storage        │ Edit          │
# │ D1 Database          │ Account > D1                        │ Edit          │
# │ Pages deployment     │ Account > Cloudflare Pages          │ Edit          │
# │ DNS management       │ Zone > DNS                          │ Edit          │
# │ Worker Routes        │ Zone > Workers Routes               │ Edit          │
# │ Zone settings        │ Zone > Zone Settings                │ Read          │
# │ Cache purge          │ Zone > Cache Purge                  │ Purge         │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# QUICK SETUP (Full Access):
# - Use the "Edit Cloudflare Workers" template as a starting point
# - Add additional permissions as needed for DNS, Pages, etc.
#
# MINIMAL SETUP (Workers Only):
# - Account > Workers Scripts > Edit
# - Account > Workers KV Storage > Edit (if using KV)
# - Account > Workers R2 Storage > Edit (if using R2)
#
# ACCOUNT RESOURCES:
# - Select "All accounts" or specific account
#
# ZONE RESOURCES (for DNS/Routes/Cache):
# - Select "All zones" or specific zones you want to manage
#
# TOKEN SECURITY BEST PRACTICES:
# - Use the principle of least privilege
# - Set token expiration (90 days recommended)
# - Restrict to specific IP addresses if possible
# - Rotate tokens periodically
#
CLOUDFLARE_API_KEY=your_api_token_here

# -----------------------------------------------------------------------------
# OPTIONAL: Cloudflare Account ID
# -----------------------------------------------------------------------------
# If not set, the account ID will be auto-detected from your API token.
# Setting this explicitly can speed up API calls slightly.
#
# Find your Account ID:
# 1. Go to https://dash.cloudflare.com/
# 2. Select your account
# 3. Look in the right sidebar under "Account ID"
#
# CLOUDFLARE_ACCOUNT_ID=your_account_id_here

# -----------------------------------------------------------------------------
# OPTIONAL: Cloudflare Email (for legacy API key authentication)
# -----------------------------------------------------------------------------
# Only needed if using Global API Key instead of API Token (not recommended)
#
# CLOUDFLARE_EMAIL=your_email@example.com
